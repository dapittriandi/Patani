<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Patani" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- Warna status bar -->
        <item name="android:statusBarColor">@color/greyBar</item>
        <!-- Warna navigasi bar (opsional) -->
        <item name="android:navigationBarColor">@color/black</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Base.Theme.Patani" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
</resources>